<div class="container">
<h1>Categories</h1>

<div id="categories">
  <% @categories.each do |category| %>
    <%= render category %>
    <p>
      <%= link_to "Show this category", category, class:"btn btn-secondary" %>
    </p>
  <% end %>
</div>
</div>